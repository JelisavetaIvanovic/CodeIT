import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [
    { path: 'game', component: AppComponent },
    { path: 'game/add-army', component: AppComponent },
];
